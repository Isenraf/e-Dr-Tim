@model IEnumerable<BANA.Models.Facture>

@{
    ViewData["Title"] = "Cautions";
    int i=1;
}


<!-- App container starts -->
        <div class="app-container">
          <!-- App hero header starts -->
          <div class="app-hero-header d-flex align-items-center">
            <!-- Breadcrumb starts -->
            <ol class="breadcrumb">
              <li class="breadcrumb-item">
                <i class="ri-home-8-line lh-1 pe-3 me-3 border-end"></i>
                <a asp-action="Index" asp-controller="Departement">Acceuil</a>
              </li>
              <li class="breadcrumb-item text-primary" aria-current="page">
                Cautions
              </li>
            </ol>
            <!-- Breadcrumb ends -->
          </div>
          <!-- App Hero header ends -->

          <!-- app body start -->
          <div class="app-body">
            <div class="card py-3 px-3">
              <div
                class="d-flex align-items-center justify-content-between mt-2"
              >
                <h5 class="card-title">Cautions</h5>

                <div class="d-flex flex-wrap gap-1">
                  <div
                    class="d-flex align-items-center box-shadow px-3 py-1 rounded-2"
                  >
                    <i class="ri-pie-chart-2-fill text-danger fs-4"></i>
                    <span class="me-1 fw-semibold ps-1">@Model.Count()</span>
                    <span class="text-danger">Total facture</span>
                  </div>
                  <div
                    class="d-flex align-items-center box-shadow px-3 py-1 rounded-2"
                  >
                    <i class="ri-pie-chart-2-fill text-success fs-4"></i>
                    <span class="me-1 fw-semibold ps-1"><span class="separateur">@ViewBag.montanttotal</span> FCFA</span>
                    <span class="text-success">Montant Total</span>
                  </div>
                  <div
                    class="d-flex align-items-center box-shadow px-3 py-1 rounded-2"
                  >
                    <i class="ri-pie-chart-2-fill text-danger fs-4"></i>
                    <span class="me-1 fw-semibold ps-1">@ViewBag.encours</span>
                    <span class="text-danger">En cours</span>
                  </div>
                  <div
                    class="d-flex align-items-center box-shadow px-3 py-1 rounded-2"
                  >
                    <i class="ri-pie-chart-2-fill text-info fs-4"></i>
                    <span class="me-1 fw-semibold ps-1">@ViewBag.fini</span>
                    <span class="text-info">Terminées</span>
                  </div>
                </div>
              </div>
              <br />

              <div class="row gx-3 gy-3">
                <div class="card">
                  <div
                    style="
                      display: flex;
                      justify-content: space-between;
                      align-items: center;
                    "
                  >
                    <div>
                      <a href="javascript:void(0);" onclick="history.back();" style="margin-right: 10px" class="text-primary"
                        >Toutes les Factures</a
                      >
                      <a asp-action="Index5" asp-route-Etat="En attente Paiement" asp-route-date="@ViewData["date"]" style="margin-right: 10px">Factures en cours</a>
                      <a asp-action="Index5" asp-route-Etat="Cloturé" asp-route-date="@ViewData["date"]" style="margin-right: 10px">Factures réglées</a>
                    </div>

                    <div style="display: flex; gap: 10px">
                      
                    </div>
                  </div>
                </div>
                <div class="col-12">
                  <div class="row gx-3 gy-3">
                    <div class="card">
                      <div class="card-body">
                        <form asp-controller="Facture" asp-action="Index"  method="get">
                          <input type="search" id="search" name="SearchString" class="form-control" placeholder="Bare de Recherche"/>
                        </form>
                      </div>
                    </div>
                  </div>

                  <div class="table-responsive mt-3">
                    <table
                      class="table truncate m-0"
                      style="width: 100%; border-collapse: collapse"
                    >
                      <thead>
                        <tr>
                          <th>#</th>
                          <th>Coordonnees du client</th>
                          <th>Date</th>
                          <th>MONTANT</th>
                          <th>Etat</th>
                          <th>Action</th>
                        </tr>
                      </thead>
                      <tbody id="testfiltre">
                          @foreach (var item in Model)
                          {
                          i=(i>8)?1:i;
                          string img="avatar"+i+".jpg";

                            <tr class="montest">
                              <td>@item.Id</td>
                              <td>
                                <div class="d-flex align-items-center">
                                  <img
                                    src="/template/assets/images/xs/@img"
                                    class="img-shadow img-2xx me-2 rounded-circle"
                                    alt="Hong Kong"
                                  />
                                  <div class="ml-3">
                                    @if (item.Type.Contains("medicament"))
                                    {
                                      <a asp-action="Details3" asp-route-Id="@item.Id" style="color: #126aef;" title="afficher la facture">Facture Nº @(item.Numero_de_facture), Dossier Nº @item.Numero_dossier</a>
                                    }else if(item.Type.Contains("assurance")){
                                      <a asp-action="Details4" asp-route-Id="@item.Id" style="color: #126aef;" title="afficher la facture">Facture Nº @(item.Numero_de_facture), Dossier Nº @item.Numero_dossier</a>
                                    }else{
                                      <a asp-action="Details" asp-route-Id="@item.Id" style="color: #126aef;" title="afficher la facture">Facture Nº @(item.Numero_de_facture), Dossier Nº @item.Numero_dossier</a>
                                    }
                                    <p style="max-width: 300px; overflow: hidden" class="mb-0">@item.Nom @item.Prenom</p>
                                  </div>
                                </div>
                                
                              </td>
                              <td>@item.Create_date.ToString("dd/MM/yyyy") @item.Create_date.ToShortTimeString()</td>
                              <td><span class="separateur">@item.Net_a_payer_patient</span> FCFA</td>
                              <td>
                                @if (item.Etat_patient=="Cloturé"){<span class="badge border border-success text-success">@item.Etat_patient</span>}else if(item.Etat_patient=="Paiement partiel"){<span class="badge border border-warning text-warning">@item.Etat_patient</span>}else{<span class="badge border border-danger text-danger">@item.Etat_patient</span>}
                              </td>
                              <td>
                                @if (item.Etat_patient=="En attente Paiement")
                                  {
                                    <button onclick="location.href='/Facture/Edit3/@item.Id?nd=@item.Numero_dossier'+'&target=@item.Type';" type="button" class="btn btn-outline-secondary" title="modifier la facturee">
                                      <i class="fa fa-edit"></i>
                                    </button>
                                  }    
                                    <button onclick="location.href='/Facture/Details/@item.Id';" type="button" class="btn btn-outline-secondary " title="Voir">
                                      <i class="fa fa-eye"></i>
                                    </button>
                              </td>
                            </tr>
                          }

                        

                      </tbody>
                    </table>
                  </div>

                  <!-- body body end -->
                 
                </div>
              </div>
              <!-- App body ends -->
            </div>
          </div>




        